<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.femgr.datasetscreatorandorganizer.MainFragment"
        android:label="ML Data Builder"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_mainFragment_to_datasetViewerFragment"
            app:destination="@id/datasetViewerFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out" />
    </fragment>

    <fragment
        android:id="@+id/datasetViewerFragment"
        android:name="com.femgr.datasetscreatorandorganizer.DatasetViewerFragment"
        android:label="Dataset Viewer"
        tools:layout="@layout/activity_dataset_viewer" />

</navigation>